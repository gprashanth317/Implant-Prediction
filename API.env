# Database and App Environment Configuration
# Replace DATABASE_URL with your secret connection string (e.g. from Neon / PostgreSQL dashboard)
DATABASE_URL=sqlite:///fallback_local.db
SECRET_KEY=change_this_to_a_secure_random_secret_key_in_production